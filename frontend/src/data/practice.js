export const PROBS = {
  arrays: [
    { id: "p1", name: "Two Sum", diff: "Easy", desc: "Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target.", hint: "Can we use a Hash Map to store the difference (target - current) as we iterate?", explain: "Iterate through the array. For each element, check if (target - num) exists in the map. If it does, you found the pair. If not, add the current number to the map." },
    { id: "p2", name: "Best Time to Buy and Sell Stock", diff: "Easy", desc: "You want to maximize your profit by choosing a single day to buy one stock and choosing a different day in the future to sell that stock.", hint: "Keep track of the minimum price seen so far.", explain: "Initialize minPrice to infinity and maxProfit to 0. Iterate through the prices. Update minPrice if the current price is lower. Else, update maxProfit if (current price - minPrice) is greater than maxProfit." },
    { id: "p2b", name: "Maximum Subarray", diff: "Medium", desc: "Given an integer array nums, find the contiguous subarray (containing at least one number) which has the largest sum and return its sum.", hint: "Kadane's Algorithm: Should we add the current number to our running sum, or start a new running sum from the current number?", explain: "Maintain a current sum. For each element, currentSum = max(num, currentSum + num). Keep track of the global max." },
    { id: "p2c", name: "Container With Most Water", diff: "Medium", desc: "Given n non-negative integers representing heights of lines, find two lines that together with the x-axis forms a container that holds the most water.", hint: "Use two pointers, one at the beginning and one at the end. Which pointer should move?", explain: "Start pointers at ends. Calculate area. To potentially increase area, move the pointer that points to the shorter line inward." }
  ],
  strings: [
    { id: "s1", name: "Valid Anagram", diff: "Easy", desc: "Given two strings s and t, return true if t is an anagram of s, and false otherwise.", hint: "Can you count the frequency of each character?", explain: "Use a hash map or an array of size 26 to count character frequencies of the first string. Decrement for the second string. Check if all counts are 0." },
    { id: "s2", name: "Longest Substring Without Repeating Characters", diff: "Medium", desc: "Given a string s, find the length of the longest substring without repeating characters.", hint: "Use a sliding window. What data structure can keep track of characters inside the window?", explain: "Use two pointers (left and right) and a Set. Expand the right pointer. If the character is in the set, remove the left character and increment the left pointer until the duplicate is gone." },
    { id: "s3", name: "Longest Palindromic Substring", diff: "Medium", desc: "Given a string s, return the longest palindromic substring in s.", hint: "Can you expand around the center of every possible palindrome?", explain: "Iterate through each character. Treat it as the center of a palindrome (both odd and even length). Expand outwards as long as it's a palindrome and keep track of the max length." }
  ],
  linkedlists: [
    { id: "p3", name: "Reverse Linked List", diff: "Easy", desc: "Given the head of a singly linked list, reverse the list, and return the reversed list.", hint: "Use three pointers: prev, curr, and next.", explain: "Initialize prev to null, curr to head. In a loop, store curr.next, point curr.next to prev, move prev to curr, and move curr to the stored next." },
    { id: "l2", name: "Merge Two Sorted Lists", diff: "Easy", desc: "Merge two sorted linked lists and return it as a sorted list.", hint: "Use a dummy head node to simplify the logic.", explain: "Create a dummy node. Compare the heads of both lists. Attach the smaller node to the dummy list and advance that pointer. Attach any remaining nodes at the end." },
    { id: "l3", name: "Linked List Cycle", diff: "Easy", desc: "Determine if a linked list has a cycle in it.", hint: "Floyd's Tortoise and Hare algorithm.", explain: "Use a slow pointer (moves 1 step) and a fast pointer (moves 2 steps). If they ever meet, there is a cycle. If fast reaches null, there is no cycle." }
  ],
  trees: [
    { id: "p4", name: "Maximum Depth of Binary Tree", diff: "Easy", desc: "Given the root of a binary tree, return its maximum depth.", hint: "Recursively find the max depth of the left and right subtrees.", explain: "Base case: if root is null, return 0. Otherwise, return 1 + max(maxDepth(root.left), maxDepth(root.right))." },
    { id: "t2", name: "Invert Binary Tree", diff: "Easy", desc: "Given the root of a binary tree, invert the tree, and return its root.", hint: "Swap the left and right children for every node.", explain: "Recursively call invertTree on the left and right children. Then swap root.left and root.right." },
    { id: "t3", name: "Lowest Common Ancestor of a BST", diff: "Medium", desc: "Given a BST, find the lowest common ancestor (LCA) node of two given nodes p and q.", hint: "Utilize the BST property: left is smaller, right is larger.", explain: "Start at the root. If both p and q are smaller, move left. If both are larger, move right. If one is smaller and one is larger (or one equals the root), you've found the LCA." }
  ],
  graphs: [
    { id: "g1", name: "Number of Islands", diff: "Medium", desc: "Given an m x n 2D binary grid grid which represents a map of '1's (land) and '0's (water), return the number of islands.", hint: "How can you traverse the entire island when you find a piece of land?", explain: "Iterate through the grid. When you find a '1', increment the island count and run DFS/BFS to turn all connected '1's into '0's to mark them as visited." },
    { id: "g2", name: "Course Schedule", diff: "Medium", desc: "Given numCourses and an array of prerequisites, determine if it is possible to finish all courses.", hint: "This is a cycle detection problem in a directed graph.", explain: "Build an adjacency list. Use DFS with a 'visiting' state to detect cycles (Topological Sort). Alternatively, use Kahn's Algorithm (BFS) counting in-degrees." }
  ],
  dp: [
    { id: "p5", name: "Climbing Stairs", diff: "Easy", desc: "You are climbing a staircase. It takes n steps to reach the top. Each time you can either climb 1 or 2 steps. In how many distinct ways can you climb to the top?", hint: "This is a Fibonacci sequence.", explain: "Base cases: dp[1]=1, dp[2]=2. For i > 2, dp[i] = dp[i-1] + dp[i-2]." },
    { id: "d2", name: "Coin Change", diff: "Medium", desc: "Given an integer array coins representing coins of different denominations and an integer amount, return the fewest number of coins that you need to make up that amount.", hint: "This is a classic DP problem. What is the minimum coins to make amount X if we pick coin C?", explain: "Create a dp array of size amount + 1 initialized to infinity. dp[0] = 0. For each amount up to target, for each coin, dp[amount] = min(dp[amount], dp[amount - coin] + 1)." },
    { id: "d3", name: "Longest Increasing Subsequence", diff: "Medium", desc: "Given an integer array nums, return the length of the longest strictly increasing subsequence.", hint: "For each element, find the longest subsequence that ends with an element smaller than the current one.", explain: "Create a dp array of size n initialized to 1. For each i, check all j < i. If nums[i] > nums[j], dp[i] = max(dp[i], dp[j] + 1)." }
  ]
};
